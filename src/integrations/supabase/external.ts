// Backend client — uses the project's internal backend (Lovable Cloud).
// No external Supabase connection.
export { supabase } from "./client";
export { supabase as supabaseExternal } from "./client";
